# 新博客后端开发约定

## 项目职责

- Go + PostgreSQL；负责认证、内容管理、媒体、构建快照及发布任务。
- PostgreSQL 是新系统文章的唯一权威来源；归档仓库不是第二个写入源。
- 分开 HTTP、业务和数据库访问，使用参数化 SQL及版本化迁移。
- 不引入微服务、Redis 或独立消息队列；不依赖相邻前端仓库运行自身检查。

## 权限与数据

- 每个管理 API 都鉴权，Cookie 写操作做 CSRF 防护。
- 构建身份与管理员权限分离，不记录密码、token 或连接串。
- 上传校验实际文件内容、尺寸和路径，限制处理并发。
- 区分草稿、待发布修订和已上线快照；快照冻结完整输入。
- 发布任务持久化，支持幂等、超时、重试和状态对账。
- Actions 提交成功不代表上线；旧任务不可覆盖新版本。
- 归档失败单独处理，不将已上线网站误报为发布失败。

## 兼容与运维

- API 契约在本仓库维护，变更同步文档和相关测试。
- 新后端兼容仍在线的旧静态前端，关联前端变更。
- 不改写已应用迁移；升级考虑旧数据与回滚兼容性。
- 普通开发任务不自动执行生产迁移或删除数据。
- 服务监听本机，连接池和资源有界；发文章不重新部署后端。
- VPS 同时运行代理，保留 Caddy/sing-box，不在其上构建 Nuxt。

## 验证

- 当前 Go 1.25.0、PostgreSQL 18（本机 18.6），依赖以 go.mod/go.sum 为准，启动与迁移命令见 README。
- 集成测试使用专用数据库，不使用生产数据库。
- 覆盖鉴权、事务、修订隔离、发布幂等、乱序、失败和回调丢失。

## 第一轮实际实现与检查

- 已实现认证、不可变修订、净化预览、不可变快照及独立构建身份，契约见 docs/API.md。尚无 publish worker、部署状态机、媒体或生产部署；不要把快照创建当成上线。
- HTTP 位于 internal/httpapi，业务在 internal/service、internal/domain，SQL 在 internal/repository；迁移内嵌并记录校验和，不修改已应用 SQL。
- 使用 `gofmt -l cmd internal`、`go vet ./...`、`go test ./...`。完整数据库验收必须执行 `scripts/test.ps1`；普通 go test 未配置测试环境时会显式跳过数据库测试。
- 测试脚本只允许 127.0.0.1 的 shuntian_blog_test 与专用 app/owner，追加测试数据，不清空开发库。初始化拒绝接管未知同名数据库或角色。
- `.local/` 保存本机凭据、PID 和日志并被忽略，禁止输出密码、token、连接串。没有默认管理员密码。

## 协作与版权

- 默认用中文说明改动和实际验证结果，区分计划与已实现功能。
- 尊重已有修改，不进行无关重构、目录移动或 Git 初始化。
- 本地开发不自动授权生产部署、服务重启或数据删除；已有明确授权不重复询问。
- 作者原创内容暂不授权他人复用，不擅自添加开源许可证；保留第三方许可、署名和引用。
- 命令与版本以本仓库实际文件为准，未执行的检查不得报告通过。
